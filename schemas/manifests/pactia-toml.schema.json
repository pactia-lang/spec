{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pactia.io/schemas/manifests/pactia-toml.schema.json",
  "title": "pactia.toml (parsed)",
  "description": "Logical shape after parsing pactia.toml. Source is TOML on disk; see spec/docs/packages.md.",
  "type": "object",
  "properties": {
    "workspace": {
      "type": "object",
      "properties": {
        "entry": {
          "type": "string",
          "minLength": 1,
          "description": "Product entry file relative to project root"
        },
        "members": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Optional module directory paths for monorepos"
        }
      },
      "additionalProperties": false
    },
    "stackPackage": {
      "type": "string",
      "description": "Stack-kind package coordinate bound by product @stack",
      "pattern": "^@[\\w-]+/[\\w-]+$"
    },
    "dependencies": {
      "type": "object",
      "description": "Semver ranges keyed by package coordinate",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "additionalProperties": false
}
