{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pactia.io/schemas/manifests/pactia-toml.schema.json",
  "title": "pactia.toml (parsed)",
  "description": "Logical shape after parsing pactia.toml. See spec/docs/packages.md. [package] name, version, optional description, plus [dependencies] only — Cargo-style, no kind field.",
  "type": "object",
  "properties": {
      "package": {
      "type": "object",
      "description": "Product or package identity",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "version": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string"
        },
        "exports": {
          "type": "string",
          "enum": ["registry", "topology"],
          "default": "registry",
          "description": "Declared export profile; defaults to 'registry'. Topology packages declare 'topology'."
        },
        "mixed-exports": {
          "type": "boolean",
          "default": false,
          "description": "Opt-in for packages exporting both registry defs and topology symbols. Prefer separate packages."
        }
      },
      "additionalProperties": false
    },
    "dependencies": {
      "type": "object",
      "description": "Semver ranges keyed by package coordinate (@scope/name)",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "additionalProperties": false
}
