{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pactia.io/schemas/manifests/pactia-lock.schema.json",
  "title": "pactia.lock",
  "description": "Logical shape after parsing pactia.lock. On disk, TOML uses Cargo-style [[package]] tables; see spec/docs/packages.md. Omit lockfile entirely for products with no package imports.",
  "type": "object",
  "properties": {
    "lockVersion": {
      "type": "integer",
      "minimum": 1
    },
    "packages": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "version", "digest"],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^@[\\w-]+/[\\w-]+$"
          },
          "version": {
            "type": "string",
            "minLength": 1
          },
          "digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
